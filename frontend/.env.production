# Production build: API is reachable on the same origin via nginx reverse proxy.
# nginx routes /api/* → http://127.0.0.1:8000/* (the FastAPI container).
# This file is committed: no secrets here, Vite inlines VITE_* into the bundle.
VITE_API_BASE_URL=/api
